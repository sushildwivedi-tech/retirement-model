{
  "name": "retirement-model",
  "private": true,
  "version": "0.1.0",
  "description": "Personal retirement modelling for an Australian household. Phase 1: deterministic core.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=@retirement/engine && npm run test --workspace=web && npx vitest run --root apps/desktop",
    "typecheck": "npm run typecheck --workspace=@retirement/engine",
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "app": "npm run build --workspace=web && node apps/desktop/server.ts",
    "app:serve": "node apps/desktop/server.ts"
  }
}
